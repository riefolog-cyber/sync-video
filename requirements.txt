# Dipendenze principali
pymupdf>=1.23.0
pillow>=10.0.0
pytesseract>=0.3.10
pydub>=0.25.0
vosk>=0.3.45
moviepy>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0

# Fallback semantico (sincronizzazione via embeddings, senza LLM)
fastembed>=0.8.0

# Selezione slide via LLM opzionale (9Router online, endpoint OpenAI-compatibile)
requests>=2.31.0

# Opzionale: transcriber whisper (python main.py --transcriber whisper)
faster-whisper>=1.0.0

# Dipendenze di sviluppo
mypy>=1.10.0
ruff>=0.16.0