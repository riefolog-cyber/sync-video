# Dipendenze principali
pymupdf>=1.23.0
pillow>=10.0.0
pytesseract>=0.3.10
pydub>=0.25.0
moviepy>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0

# Fallback semantico (sincronizzazione via embeddings, senza LLM)
# Pinnata a 0.5.1: le versioni successive usano mean pooling invece di CLS
# per e5-large, cambiando gli embedding rispetto alla baseline validata (A/B).
# A/B re-test 2026-08 vs 0.8.0 su dati reali (check_fastembed_upgrade.py):
# coseno medio 0.935, decision match 47% -> VERDETTO DIVERGENTE, pin confermato.
fastembed==0.5.1


# Selezione slide via LLM opzionale (9Router online, endpoint OpenAI-compatibile)
requests>=2.31.0

# Trascrizione audio (faster-whisper, fallback CPU)
faster-whisper>=1.0.0

# Trascrizione veloce su iGPU Intel (opzionale ma raccomandato):
openvino>=2025.1.0
openvino-genai>=2025.1.0

# Dipendenze di sviluppo
mypy>=1.10.0
ruff>=0.16.0